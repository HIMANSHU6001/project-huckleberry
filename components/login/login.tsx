import { Button } from "../ui/button";

export const Login = () => {
  return (
    <div className="h-screen flex justify-center items-center bg-black">
      <Button variant="default">loin with google</Button>
    </div>
  );
};
